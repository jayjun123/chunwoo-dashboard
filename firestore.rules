rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 모드 - 모든 인증된 사용자에게 모든 권한 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 