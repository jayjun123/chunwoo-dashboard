warning: in the working copy of 'src/components/TodoList.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/components/TodoList.jsx b/src/components/TodoList.jsx[m
[1mindex 2be4312..e9d2120 100644[m
[1m--- a/src/components/TodoList.jsx[m
[1m+++ b/src/components/TodoList.jsx[m
[36m@@ -26,6 +26,7 @@[m [mimport CheckCircleIcon from '@mui/icons-material/CheckCircle';[m
 import CancelIcon from '@mui/icons-material/Cancel';[m
 import ScheduleIcon from '@mui/icons-material/Schedule';[m
 import { useAuth } from '../contexts/AuthContext';[m
[32m+[m[32mimport { useTodo } from '../contexts/TodoContext';[m
 import { isMasterUser } from '../utils/masterUtils';[m
 import * as XLSX from 'xlsx';[m
 import { format, startOfDay, endOfDay, isToday, isYesterday, subDays } from 'date-fns';[m
[36m@@ -38,7 +39,7 @@[m [mconst statusColor = (completed, planned) => {[m
 };[m
 [m
 const TodoList = () => {[m
[31m-  const [todos, setTodos] = useState([]);[m
[32m+[m[32m  const { todos, loading, error, addTodo, updateTodo, deleteTodo, toggleTodo } = useTodo();[m
   const [allUsers, setAllUsers] = useState([]);[m
   const [selectedUser, setSelectedUser] = useState(null);[m
   const [selectedDate, setSelectedDate] = useState(format(new Date(), 'yyyy-MM-dd'));[m
[36m@@ -75,37 +76,8 @@[m [mconst TodoList = () => {[m
     }[m
   }, [userId, isMaster]);[m
 [m
[31m-  useEffect(() => {[m
[31m-    if (!userId) return;[m
[31m-    [m
[31m-    let q;[m
[31m-    const targetUserId = isMaster && selectedUser ? selectedUser : userId;[m
[31m-    [m
[31m-    if (isMaster && selectedUser) {[m
[31m-      // 마스터 계정이 특정 사용자 선택 시[m
[31m-      q = query([m
[31m-        collection(db, collections.todos),[m
[31m-        where('userId', '==', targetUserId),[m
[31m-        orderBy('createdAt', 'desc')[m
[31m-      );[m
[31m-    } else {[m
[31m-      // 일반 사용자 또는 마스터 계정이 자신의 투두리스트[m
[31m-      q = query([m
[31m-        collection(db, collections.todos),[m
[31m-        where('userId', '==', targetUserId),[m
[31m-        orderBy('createdAt', 'desc')[m
[31m-      );[m
[31m-    }[m
[31m-    [m
[31m-    const unsubscribe = onSnapshot(q, (snapshot) => {[m
[31m-      const data = snapshot.docs.map(doc => ({ id: doc.id, ...doc.data() }));[m
[31m-      console.log('투두 데이터 업데이트:', data.length, '개');[m
[31m-      console.log('투두 데이터:', data);[m
[31m-      setTodos(data);[m
[31m-    });[m
[31m-    [m
[31m-    return () => unsubscribe();[m
[31m-  }, [userId, isMaster, selectedUser]);[m
[32m+[m[32m  // TodoContext에서 todos 데이터를 가져오므로 별도 쿼리 불필요[m
[32m+[m[32m  // 마스터 계정의 경우 selectedUser에 따른 필터링은 UI에서 처리[m
 [m
   // 일자별 그룹핑[m
   const grouped = todos.reduce((acc, todo) => {[m
[36m@@ -162,25 +134,15 @@[m [mconst TodoList = () => {[m
     const todoText = newTodos[date] || '';[m
     if (!todoText.trim()) return;[m
     [m
[31m-    const targetUserId = isMaster && selectedUser ? selectedUser : userId;[m
[31m-    [m
     console.log('투두 추가 시도:', {[m
       text: todoText.trim(),[m
[31m-      date: date,[m
[31m-      userId: targetUserId[m
[32m+[m[32m      date: date[m
     });[m
     [m
     try {[m
[31m-      const docRef = await addDoc(collection(db, collections.todos), {[m
[31m-        text: todoText.trim(),[m
[31m-        completed: false,[m
[31m-        userId: targetUserId,[m
[31m-        date: date,[m
[31m-        createdAt: new Date(),[m
[31m-        carriedOver: false[m
[31m-      });[m
[32m+[m[32m      await addTodo(todoText.trim(), date);[m
       [m
[31m-      console.log('투두 추가 성공:', docRef.id);[m
[32m+[m[32m      console.log('투두 추가 성공');[m
       // 해당 날짜의 입력창 초기화[m
       setNewTodos(prev => ({[m
         ...prev,[m
[36m@@ -198,7 +160,7 @@[m [mconst TodoList = () => {[m
   // 투두 삭제[m
   const handleDeleteTodo = async (id) => {[m
     try {[m
[31m-      await deleteDoc(doc(db, collections.todos, id));[m
[32m+[m[32m      await deleteTodo(id);[m
     } catch (error) {[m
       console.error('투두 삭제 오류:', error);[m
     }[m
[36m@@ -208,9 +170,7 @@[m [mconst TodoList = () => {[m
   const handleToggleTodo = async (id) => {[m
     try {[m
       const todo = todos.find(t => t.id === id);[m
[31m-      await updateDoc(doc(db, collections.todos, id), {[m
[31m-        completed: !todo.completed[m
[31m-      });[m
[32m+[m[32m      await toggleTodo(id, !todo.completed);[m
     } catch (error) {[m
       console.error('투두 상태 변경 오류:', error);[m
     }[m
[36m@@ -226,9 +186,7 @@[m [mconst TodoList = () => {[m
   // 투두 저장[m
   const handleSaveEdit = async (id) => {[m
     try {[m
[31m-      await updateDoc(doc(db, collections.todos, id), {[m
[31m-        text: editText[m
[31m-      });[m
[32m+[m[32m      await updateTodo(id, { text: editText });[m
       setEditingId(null);[m
       setEditText('');[m
     } catch (error) {[m
